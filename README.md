# Website

This website is built using [Docusaurus](https://docusaurus.io/), a modern static website generator.

The production site is deployed as a Cloudflare Worker with static assets served from the Docusaurus `build` output.
